export interface VulnSummary {
    cve: string,
    title: string,
    release: string,
    description: string,
    cvss: number,
    // affected: Vec<String>,
    // fixed: Vec<String>,
    // advisories: Vec<String>,
  }
  